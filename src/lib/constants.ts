export const SECTION_IDS = {
  hero: "hero",
  about: "about",
  projects: "projects",
  skills: "skills",
  contact: "contact",
} as const;

export const ANIMATION_DEFAULTS = {
  duration: 0.5,
  staggerDelay: 0.1,
  yOffset: 40,
} as const;
